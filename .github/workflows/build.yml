name: Build IPK

on:
  push:
    branches:
      - go-v2.0
      - main
  pull_request:
    branches:
      - go-v2.0
      - main
  workflow_dispatch:

jobs:
  build:
    name: ImmortalWrt 24.10 ${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64-24.10.6

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build packages
        uses: immortalwrt/gh-action-sdk@master
        env:
          ARCH: ${{ matrix.arch }}
          PACKAGES: fileshare luci-app-fileshare
          NO_SHFMT_CHECK: 1
          V: s

      - name: Upload IPK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-24.10-${{ matrix.arch }}-ipk
          path: bin/packages/*/action/*.ipk
          if-no-files-found: error
